{{- if and (eq .Values.installationMode "ArgoCD") .Release.IsInstall -}}
apiVersion: v1
kind: Secret
metadata:
  name: mobsf-secret
type: Opaque
data:
  mobsfapikey: {{ randAlphaNum 62 | b64enc | quote }}
{{- end }}
