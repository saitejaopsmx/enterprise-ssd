{{- if eq .Values.mode "standalone" }}
{{- if and .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "minio.fullname" . }}
  labels:
    app: {{ template "minio.name" . }}
    chart: {{ template "minio.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
{{- if and .Values.nasgateway.enabled .Values.nasgateway.pv }}
  selector:
    matchLabels:
      pv: {{ .Values.nasgateway.pv | quote }}
{{- end }}
  accessModes:
    - {{ .Values.global.minio.persistence.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.global.minio.persistence.size | quote }}
{{- if .Values.global.minio.persistence.storageClass }}
{{- if (eq "-" .Values.global.minio.persistence.storageClass) }}
  storageClassName: ""
{{- else }}
  storageClassName: "{{ .Values.global.minio.persistence.storageClass }}"
{{- end }}
{{- end }}
{{- if .Values.global.minio.persistence.VolumeName }}
  volumeName: "{{ .Values.global.minio.persistence.VolumeName }}"
{{- end }}
{{- end }}
{{- end }}
