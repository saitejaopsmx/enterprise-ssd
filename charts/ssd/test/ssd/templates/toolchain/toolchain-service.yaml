---
# Source: ssd/templates/toolchain/toolchain-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: tool-chain
  labels:
    app: ssd
    component: toolchain
    heritage: "Helm"
    release: "ssd"
    chart: "ssd-2024.12.00"
spec:
  ports:
    - name: tool-chain
      protocol: TCP
      port: 8100
      targetPort: 8100
  selector:
    app: ssd
    component: toolchain
