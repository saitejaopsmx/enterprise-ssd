apiVersion: v1
kind: ConfigMap
metadata:
  name: checkmarx-poller
  labels:
    app: ssd
{{ include "ssd.standard-labels" . | indent 4 }}
    component: checkmarx-poller
data:
{{ (tpl (.Files.Glob "config/checkmarx/checkmarx-poller.yaml").AsConfig . ) | indent 2 }}
